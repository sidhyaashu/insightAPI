# name: Publish Release & Docker Build

# on:
#   push:
#     tags:
#       - "v*.*.*"

# jobs:
#   pypi-release:
#     name: Build & Publish PyPI Package
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - name: Install Build Tools
#         run: |
#           python -m pip install --upgrade pip build twine

#       - name: Build Package Wheels & Source Tarball
#         run: |
#           python -m build ./backend

#       - name: Check Package Artifacts
#         run: |
#           twine check backend/dist/*

#       # Uncomment once PYPI_API_TOKEN secret is added to repository settings
#       # - name: Publish to PyPI
#       #   env:
#       #     TWINE_USERNAME: __token__
#       #     TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
#       #   run: |
#       #     twine upload backend/dist/*

#   docker-build:
#     name: Build & Push Docker Image
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Build Local Docker Image Test
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           file: ./Dockerfile
#           push: false
#           tags: insightapi:latest

