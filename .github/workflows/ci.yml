# name: CI Test Suite

# on:
#   push:
#     branches: [ main, master ]
#   pull_request:
#     branches: [ main, master ]

# jobs:
#   test:
#     name: Run PyTest & Linting
#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         python-version: ["3.10", "3.11", "3.12", "3.13"]

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Set up Python ${{ matrix.python-version }}
#         uses: actions/setup-python@v5
#         with:
#           python-version: ${{ matrix.python-version }}
#           cache: "pip"

#       - name: Install agent-service & Dependencies
#         working-directory: services/agent-service
#         run: |
#           python -m pip install --upgrade pip
#           pip install -e ".[dev]"

#       - name: Install Playwright Browsers & System Dependencies
#         run: |
#           playwright install --with-deps chromium

#       - name: Run PyTest Suite
#         working-directory: services/agent-service
#         run: |
#           pytest tests/ -v --cov=app --cov-report=term-missing

#   migration-check:
#     name: Validate Alembic Migrations
#     runs-on: ubuntu-latest

#     services:
#       postgres:
#         image: pgvector/pgvector:pg16
#         env:
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: postgres
#           POSTGRES_DB: insightapi_ci
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd pg_isready
#           --health-interval 5s
#           --health-timeout 5s
#           --health-retries 5

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Set up Python 3.11
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"
#           cache: "pip"

#       - name: Install agent-service (for alembic + models)
#         working-directory: services/agent-service
#         run: |
#           python -m pip install --upgrade pip
#           pip install -e ".[dev]"

#       - name: Run agent-service Alembic migrations
#         working-directory: services/agent-service
#         env:
#           POSTGRES_HOST: localhost
#           POSTGRES_PORT: 5432
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: postgres
#           POSTGRES_DB: insightapi_ci
#         run: |
#           alembic upgrade head
#           echo "✓ agent-service migrations applied successfully"

#       - name: Verify no pending migrations (autogenerate check)
#         working-directory: services/agent-service
#         env:
#           POSTGRES_HOST: localhost
#           POSTGRES_PORT: 5432
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: postgres
#           POSTGRES_DB: insightapi_ci
#         run: |
#           alembic check
#           echo "✓ No pending schema drift detected"

#       - name: Install core-service
#         working-directory: services/core-service
#         run: |
#           pip install -e ".[dev]"

#       - name: Run core-service Alembic migrations
#         working-directory: services/core-service
#         env:
#           POSTGRES_HOST: localhost
#           POSTGRES_PORT: 5432
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: postgres
#           POSTGRES_DB: insightapi_ci
#         run: |
#           alembic upgrade head
#           echo "✓ core-service migrations applied successfully"
